spring:
 application:
  name: worker-service
 rabbitmq:
  host: ${host}
  port: 5672
  username: ${username}
  password: ${password}

server:
 port: 8081

app:
 rabbitmq:
  queues:
   email: notification.queue.email
