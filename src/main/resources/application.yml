spring:
 application:
  name: worker-service
 rabbitmq:
  host: ${HOST}
  port: 5672
  username: ${USERNAME}
  password: ${PASSWORD}
  virtual-host: /

server:
 port: 8081

app:
 rabbitmq:
  queues:
   email: notification.queue.email
